# =============================================================================
# cases/smoke.cmd - smoke case: run default cfg for 100ns
#
# Explicitly specify the test class (no defaulting — .cmd MUST contain `simv: +UVM_TESTNAME=...`)
# =============================================================================

simv: +UVM_TESTNAME=my_test

